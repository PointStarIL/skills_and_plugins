# CLAUDE.md — כללי עבודה ב-repo הזה

Repo זה הוא ה-marketplace הפרטי של משרד עורכי דין חיים מרכוס לפלאג-אינים וסקיילים משפטיים בעברית.
המסמך הזה מרכז את הכללים שחייבים לפעול לפיהם. לפירוט מלא — ראה **[README.md](README.md)**.

---

## 1. איך עבודה מתחילה: תיקיית `upload/`

**כל תיקון או שינוי מתחיל בקובץ שהמשתמש מכניס ל-`upload/`.** כשהמשתמש אומר "הכנסתי קובץ",
"יש תיקון", "תראה ב-upload" — קרא את התיקייה, אל תשאל איפה הקובץ.

| קובץ | מה לעשות |
|---|---|
| `*.patch` / `*.diff` | `git apply` על העץ, ואז ודא שהתוצאה תקינה |
| `*.md` / `*.txt` | תיאור חופשי של הבעיה — אתר את הקבצים בעצמך ובצע את התיקון |
| `*.plugin` / `*.skill` | ארכיוני zip של ה-watcher האוטומטי. **אל תיגע בהם ידנית** |

- הקבצים ב-`upload/` מוחרגים ב-`.gitignore` — **אל תעשה להם `git add`.** רק תוצאת התיקון נכנסת ל-repo.
- אם התיאור בקובץ לא חד-משמעי (לא ברור באיזה סקיל מדובר, או מה ההתנהגות הרצויה) — **שאל לפני שתבצע**.
- אחרי שהתיקון פורסם: דווח מה נעשה + קישור ל-PR, ו**שאל** אם למחוק את קובץ התיקון מ-`upload/`.
- `scripts/watch-upload.sh` ו-`process-upload.sh` רצים בשרת בלבד. **אל תריץ אותם מקומית** —
  הם דוחפים ישירות ל-git בלי PR.

## 2. הכלל הקריטי: בלי merge, שום דבר לא הופץ

```
קובץ ב-upload/  →  תיקון ב-branch  →  PR ל-main ב-GitHub  →  MERGE  →  הפצה לכל המחשבים
```

- **לעולם לא push ישיר ל-`main`.** תמיד branch → PR.
- **מיזוג ה-PR הוא האירוע שמפיץ את השינוי.** כל עוד ה-PR פתוח, השינוי לא קיים מבחינת אף מחשב אחר:
  - **Claude Desktop (Cowork)** מסנכרן את ה-marketplace **רק כשממוזג PR ב-GitHub** (auto-sync).
    push ל-branch, ואפילו push ישיר ל-main מ-Gitea, לא מייצרים את האירוע הזה.
  - **Claude Code (CLI)** מושך מ-`main` בפקודה `/plugin marketplace update marcus-law` ואז `/plugin update`.
- GitHub (`PointStarIL/skills_and_plugins`) הוא **מקור-האמת**. Gitea הוא mirror שמתעדכן ממנו אוטומטית.
- אחרי פתיחת PR — אמור למשתמש במפורש שהוא צריך להיות ממוזג, ואל תדווח "העדכון הופץ" לפני שהוא מוזג.

## 3. ניהול גרסאות — מה באמת קובע

**גרסה אחת בלבד משפיעה על הפצה: `version` ב-`plugins/<plugin>/.claude-plugin/plugin.json`.**
Claude Code משתמש בגרסת הפלאג-אין כ-**cache key** שקובע אם קיים עדכון. סדר פתרון הגרסה,
הראשון שמוגדר מנצח:

1. `version` ב-`plugin.json`
2. `version` ברשומת ה-marketplace ב-`marketplace.json`
3. **git commit SHA** של המקור — זה מה שחל על ה-repo הזה כשאין `version`
4. sha256, למקורות archive
5. `unknown`

### החבילה היא יחידת ההפצה — לא הסקיל

זו הנקודה הקריטית כשחבילה מכילה כמה סקיילים. **אין מנגנון להפיץ סקיל בודד.** כל החבילה
נמשכת כיחידה אחת לתיקייה `~/.claude/plugins/cache/marcus-law/<plugin>/<version>/`. לכן,
כשמתקנים סקיל אחד מתוך שבעה:

- מעלים את גרסת **החבילה** פעם אחת. זהו. אין ולא צריך "לשחרר" את הסקיל בנפרד.
- כל הסקיילים בחבילה נשלחים יחד. **לא קיים** מצב שבו למשתמש סקיל א' בגרסה חדשה וסקיל ב' בישנה.
- שינוי בקובץ `references/`, ב-`agents/`, בסקריפט או ב-`plugin.json` הוא שינוי בחבילה לכל דבר,
  בדיוק כמו שינוי ב-`SKILL.md`.

### נקודת האפס — 2026-08-11

**כל החבילות אופסו ל-`1.0.0`, וכל 18 הסקיילים ל-`metadata.version: "1.0.0"`.** ההיסטוריה
שקדמה לתאריך הזה אינה מתועדת ברמת החבילה; היא קיימת ב-`git log`, ובחמישה סקיילים גם
בקובצי `CHANGELOG.md` ישנים שסומנו כארכיון.

**לכל חמש החבילות יש `version` מפורש ב-`plugin.json`. אין חבילה במצב SHA.** זה מכוון:
מצב SHA שולח כל מיזוג לכל המשתמשים אוטומטית, וזה מבטל את שער האישור שבסעיף הבא.

### מי רשאי להעלות מה — שער אישור

זהו כלל ברזל. אין לחרוג ממנו.

| רמה | מ- → אל | מי מאשר |
|---|---|---|
| **PATCH** — הספרה השלישית | 1.0.0 → 1.0.**1** | **Claude רשאי לבצע לבד**, ולדווח |
| **MINOR** — הספרה האמצעית | 1.0.1 → 1.**1**.0 | **טעון אישור מפורש של המשתמש, עם נימוק** |
| **MAJOR** — הספרה הראשונה | 1.1.0 → **2**.0.0 | **טעון אישור מפורש; שמור לשינוי שובר בלבד** |

מה נופל לאיזו רמה:

- **PATCH** — תיקון שאינו משנה *מה* הסקיל עושה: ניסוח, שגיאת כתיב, תיקון באג בסקריפט,
  חידוד הוראה קיימת, תיקון קובץ ב-`references/`, תיקון תיעוד, תיקון נתיב.
- **MINOR** — יכולת חדשה: מצב פלט חדש, סקיל חדש בחבילה, קובץ `references/` חדש שמרחיב
  התנהגות, שינוי ב-`description` שמשנה **מתי** הסקיל נטען, תלות חדשה.
- **MAJOR** — שבירת תאימות: הסרה או שינוי שם של סקיל, שינוי פורמט פלט שמסתמכים עליו,
  שינוי במנוע ה-DOCX שמשנה מסמכים שכבר נוצרו, ארגון מחדש של מבנה החבילה.

הכללים התפעוליים:

1. **PATCH — בצע ודווח.** אין צורך לשאול.
2. **MINOR — אסור להעלות בלי אישור.** הצג למשתמש שלושה דברים: מה השתנה, **למה זה MINOR
   ולא PATCH**, ומה ההשלכה. רק אחרי אישור מפורש — העלה.
3. **MAJOR — אסור להעלות בלי אישור מפורש, והנימוק חייב להיות רציני:** מה בדיוק נשבר,
   מי מושפע, ולמה לא קיימת דרך לא-שוברת להשיג את אותה מטרה.
4. **בספק — הרמה הנמוכה, ולשאול.** אם לא ברור אם שינוי הוא PATCH או MINOR, הוא MINOR עד
   שהמשתמש יאמר אחרת.
5. **שינוי שנוגע בכמה סקיילים מעלה את גרסת החבילה פעם אחת**, לפי הרמה **הגבוהה** מביניהם.
6. **לעולם אל תדלג על מספרים ואל תוריד גרסה.** 1.0.1 בא אחרי 1.0.0, לא 1.0.2.
7. אותם שלושה שערים חלים גם על `metadata.version` של סקיל שהשתנה, אבל שם זה תיעוד בלבד.

### כל שינוי חייב רשומה ב-CHANGELOG

לכל חבילה יש `plugins/<plugin>/CHANGELOG.md`.
**אין העלאת גרסה בלי רשומה, ואין שינוי מופץ בלי העלאת גרסה.**

רשומה חדשה נכנסת **בראש** הקובץ, מתחת לקו המפריד, בצורה הזו:

```markdown
## [1.0.1] - 2026-08-12

### תוקן
- `extract-appeal-claims`: תוקן ניסוח שגוי בהוראת טבלת המיפוי.
```

- כותרות מותרות: `### נוסף`, `### שונה`, `### תוקן`, `### הוסר`.
- כל שורה מתחילה בשם הסקיל שהושפע, כדי שיהיה אפשר לראות מה השתנה בתוך חבילה מרובת סקיילים.
- שינוי שאינו בסקיל מסוים (למשל ב-`plugin.json` או ב-`agents/`) — ציין את הרכיב במקום שם הסקיל.

### `version` ב-SKILL.md הוא תיעוד פנימי בלבד

`version` **אינו שדה מוכר** ב-frontmatter של SKILL.md — הוא לא בטבלת השדות של Claude Code ולא
במפרט Agent Skills (שמתיר `name`, `description`, `license`, `compatibility`, `metadata`, `allowed-tools`).
לכן גרסת הסקיל נשמרת תחת `metadata`, השדה שהמפרט ייעד במפורש לנתונים פרטיים:

```yaml
---
name: extract-appeal-claims
description: "..."
metadata:
  version: "1.0.0"
---
```

- Claude Code **אינו מפעיל שום לוגיקה** על הערך הזה. הוא מעקב היסטורי לבני אדם בלבד, ואינו
  משפיע על הפצה, על טעינה או על עדכון. **הגרסה שקובעת היא זו של החבילה.**
- **לעולם אל תשים `version:` ברמה העליונה של ה-frontmatter.** מפתח לא-חוקי מפיל בשגיאה קשה
  כל אריזה או העלאה של הסקיל ל-claude.ai: `Unexpected key(s) in SKILL.md frontmatter`.
- אל תשכפל את מספר הגרסה לגוף ה-SKILL.md (כותרת מסוג "(v3.1)" או פרק "מה חדש בגרסה X").
  מספר שאיש לא אוכף סוטה מהאמת; מקום התיעוד הוא ה-CHANGELOG של החבילה.
- מונה הסקיל ומונה החבילה **עצמאיים** ואינם חייבים להיות זהים: שינוי בסקיל אחד מעלה את
  המונה שלו ואת מונה החבילה, אך שאר הסקיילים בחבילה נשארים במקומם. **אל תיישר ביניהם.**

### שאר כללי הברזל

- **לעולם אל תגדיר `version` ברשומת ה-marketplace.** הוא מיותר, ו-`plugin.json` גובר עליו ממילא.
- **לעולם אל תחזיר חבילה למצב SHA** (כלומר, אל תמחק את `version` מ-`plugin.json`). זה עוקף
  את שער האישור ושולח כל מיזוג לכל המשתמשים.
- הורדת מספר גרסה **כן** מפעילה עדכון אצל המשתמשים, כי ההשוואה היא של זהות מחרוזת ולא של
  סדר semver. זה מה שאיפשר את איפוס נקודת האפס — אבל אין להשתמש בזה כדרך עבודה.

## 4. צ'ק-ליסט לעדכון סקיל קיים

1. ערוך את `SKILL.md` (או את הקבצים ב-`references/`).
2. אם היכולת השתנתה — **עדכן את שדה ה-`description` ב-frontmatter**. זה השדה שמחליט אם הסקיל
   ייטען בכלל, ותיאור מיושן פירושו סקיל שלא נטען כשצריך.
3. **סווג את השינוי** ל-PATCH / MINOR / MAJOR לפי סעיף 3. אם הוא אינו PATCH — **עצור וקבל
   אישור מנומק לפני שתעלה כל מספר.**
4. העלה `metadata.version` ב-`SKILL.md` שהשתנה (תיעוד), והעלה את `version` ב-`plugin.json`
   של החבילה. **השלב הזה הוא שקובע אם מישהו יקבל את התיקון.**
5. **הוסף רשומה ב-`plugins/<plugin>/CHANGELOG.md`** בראש הקובץ, עם שם הסקיל שהושפע. חובה.
6. אם השתנה מספר הסקיילים בחבילה — עדכן את ה-`description` ב-`plugin.json` וב-`marketplace.json`
   (יש שם ספירה מפורשת, למשל "כוללת 7 סקיילים וסוכן").
7. branch → commit → PR → **merge** (סעיף 2).

> קובצי `CHANGELOG.md` שנמצאים בתוך תיקיות סקיל הם **ארכיון** מלפני נקודת האפס. אל תוסיף
> להם רשומות ואל תסתמך על המספור שבהם; התיעוד השוטף הוא ברמת החבילה בלבד.

## 5. הוספת פלאג-אין חדש

ראה [README.md § "איך מוסיפים פלאג-אין חדש"](README.md#איך-מוסיפים-פלאג-אין-חדש).
בקצרה: העתק את `plugins/_template-plugin`, שם התיקייה = שדה `name` ב-`plugin.json`,
והוסף רשומה למערך `plugins` ב-`.claude-plugin/marketplace.json` עם `source` יחסי.

## 6. כללי תוכן

- **עיצוב מסמכי Word בעברית**: `hebrew-docx-engine` (בחבילת `marcus-law-drafting`) הוא
  **מקור-האמת היחיד** — RTL, פונט David, מספור, שוליים, סגנונות. כל שינוי עיצוב נעשה ב-`template.docx`
  שלו וחל על כל המסמכים. אל תשכפל לוגיקת עיצוב לסקיל אחר.
  יוצא דופן מכוון: `write-appeal-decision` שומר מימוש RTL עצמאי. ראה [README.md § מנוע DOCX משותף](README.md).
- **פורמט תאריך במסמכים שנוצרים**: `יום.חודש.שנה` **ללא אפסים מובילים**, עם שנה בת שתי
  ספרות. כך: `11.8.26`, `1.1.26`, `25.12.26`, `8.5.26`.
  **לא** `08.05.26`, **לא** `08.05.2026`, **לא** `8/5/26`.
  הכלל חל על תאריכים בתוך תוכן שנוצר (DOCX, Markdown, פלט למסך). **הוא אינו חל על שמות
  קבצים**, שבהם נשמר `DD.MM.YY` עם אפסים מובילים כדי שמיון אלפביתי יישאר כרונולוגי.
- **סקיילים נכתבים בלשון ציווי** — הוראות ל-Claude, לא הסבר למשתמש.
- שפת התיעוד והסקיילים כאן היא **עברית**.
- `description` בסקיל נכתב בגוף שלישי וכולל גם *מה* הסקיל עושה וגם *מתי* להשתמש בו.
