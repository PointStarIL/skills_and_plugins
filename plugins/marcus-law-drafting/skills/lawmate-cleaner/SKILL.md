---
name: lawmate-cleaner
description: >
  עיבוד אוטומטי של טיוטות משפטיות מ-law-mate ובניית מסמך וורד מעוצב ומלוטש
  המוכן לשימוש מיידי - ללא צורך בעריכה ידנית של סגנונות, שוליים, מספור או
  אזכורי נספחים. הסקיל מנקה את כל ההפניות שמערכת law-mate משבצת אוטומטית
  (הפניות לקבצי PDF/DOCX, סימוני LawMate בציטוטי פסיקה, הפניות חקיקה
  בסוגריים מרובעים, הפניות נספח/עמוד בסוגריים עגולים), מסיר את המעטפת
  הפורמלית של בית הדין, מסיר מקפים ארוכים שמסגירים כתיבה ע"י AI, ובונה
  מסמך על-בסיס תבנית `references/template.docx` עם סגנונות מוגדרים מראש:
  כותרות-משנה ב-Heading 2, פסקאות גוף עם מספור אוטומטי 1, 2, 3 (List
  Paragraph), אזכורי נספחים ("מצורף ומסומן כנספח N") עם קו תחתון,
  וסעדים מבוקשים ממוספרים אוטומטית באותיות א, ב, ג, ד (hebrew1 numbering).
  שמות הצדדים בציטוטי פסיקה מודגשים, וציטוט חוזר של אותו פסק דין מוסר
  (מצוטט במלואו רק בפעם הראשונה).

  טריגרים: "law-mate", "lawmate", "law mate", "טיוטה משפטית", "תעבד את
  המסמך", "תסדר את הטיוטה", "תנקה הפניות", "תכין את הטיוטה לעריכה". הפעל
  גם כשהמשתמש מצרף DOCX שיש בו הפניות בסגנון `[תיק-XX - שם.pdf, עמ' Y]` או
  `(LawMate תאריך)` או `[עבודה אזורי, ב"ל ..., פלוני נ' אלמוני, תאריך]` -
  אלו סימנים מובהקים לטיוטה מ-law-mate. עובד על כל סוג מסמך: כתב ערעור,
  כתב תביעה, כתב הגנה, בקשה, תצהיר, סיכומים.
version: "1.2.0"
---

# Lawmate Cleaner - עיבוד טיוטות משפטיות מ-law-mate

מערכת law-mate מפיקה טיוטות עם סימוני מערכת רבים שצריכים להיות מנוקים לפני
שהמסמך מוכן לעריכה. הסקיל הזה ממכן את התהליך כולו.

## מה הסקיל עושה

הסקיל לוקח קובץ DOCX מ-law-mate ומפיק קובץ DOCX חדש שמתאים לסגנון העריכה של
המשתמש. הניקוי כולל את הרבדים הבאים:

1. הסרת המעטפת הפורמלית של בית הדין שתיכנס ממילא לטמפלט של המשתמש.
2. ניקוי הפניות אוטומטיות שמערכת law-mate מוסיפה לקבצי המקור.
3. הצגת ציטוטי פסיקה בפורמט נקי בלי הסימון "LawMate".
4. הסרת מקפים ארוכים (em-dash: `—`) שמסגירים כתיבה ע"י בינה מלאכותית
   והחלפתם במקף רגיל (`-`).
5. הסרת הפניות לנספחים בסוגריים עגולים בסוף פסקה (כגון `(1)`, `(12)`).
6. הדגשת שמות הצדדים בציטוטי פסיקה (Bold).
7. בנייה מחדש של המסמך בעיצוב אחיד עם חלוקה ברורה לפרקים ראשיים,
   תת-כותרות, וסעיפים ממוספרים.

## איך להפעיל

הסקיל ממומש כסקריפט Python יחיד. כשהמשתמש מצרף טיוטה מ-law-mate, הפעל אותו
כך:

```bash
python3 <נתיב_הסקיל>/scripts/clean_lawmate.py "<input.docx>" "<output.docx>"
```

אם נתיב הפלט לא ניתן, הסקריפט יוסיף `_מסודר.docx` לשם הקובץ המקורי וישמור
באותה תיקייה. הסקריפט מטפל בכל הניקוי, זיהוי הפרקים, וייצור המסמך החדש.
לאחר ההפעלה, הוא מדפיס סיכום של מה שבוצע (כמה הפניות נוקו, כמה פרקים זוהו,
כמה מקפים ארוכים נוקו, וכמה ציטוטים הודגשו).

## מה הסקריפט עושה אוטומטית

**הסרת המעטפת הפורמלית.** מערכת law-mate מוסיפה בראש כל מסמך בלוק עם שם בית
הדין, מספר תיק, פרטי הצדדים, "מהות התביעה", וסעיפים מקדמיים סטנדרטיים (שווי
תובענה, אגרה, רשימת נספחים). כל אלו יוסרו, כיוון שהמשתמש משתמש בטמפלט קבוע
שדואג למעטפת. הסקריפט מזהה את סוף המעטפת לפי הופעת הכותרת הראשית הראשונה
(בדרך כלל "תמצית טענות") או הכותרת האמיתית הראשונה במסמך.

**ניקוי הפניות.** מערכת law-mate משבצת לאורך הטקסט הפניות אוטומטיות בסוגריים
מרובעים. הסקריפט מנקה את כולן:

- `[תיק-XX - שם המסמך.pdf, עמ' Y]` - מוסר לחלוטין.
- `[שם_מסמך.docx, עמ' Y]` - מוסר לחלוטין.
- `[חוק X, סעיף Y]` - מוסר לחלוטין (החוק מוזכר ממילא בזרימת הטקסט).
- ציטוטי פסיקה בסגנון `[עב"ל 91/99 פלוני נ' אלמוני (LawMate 17.1.2002)]` -
  הופכים ל-`עב"ל 91/99 **פלוני נ' אלמוני** (17.1.2002)` עם הדגשת שמות
  הצדדים.
- ציטוטי פסיקה בפורמט החדש `[עבודה אזורי, ב"ל 32911-11-23, אריאלה לסנר
  נ. המוסד לביטוח לאומי, 21.1.2025]` - הופכים ל-`(עבודה אזורי, ב"ל
  32911-11-23, **אריאלה לסנר נ. המוסד לביטוח לאומי**, 21.1.2025)` עם הדגשת
  שמות הצדדים. ציטוט עם שמות צדדים ריקים (placeholder) - נמחק כליל.

**הסרת מקפים ארוכים.** מערכת law-mate (וכל מודל בינה מלאכותית בכלל) משתמשת
לעיתים במקף ארוך (`—`, U+2014) כסימן פיסוק. המשתמש מסתייג מסימן זה מכיוון
שהוא חושף את העובדה שהמסמך נכתב על-ידי בינה מלאכותית. הסקריפט מחליף את כל
המקפים הארוכים (`—`) ואת מקפי האמצע (`, `) במקף רגיל (`-`).

**הסרת הפניות לנספחים.** מערכת law-mate מוסיפה לעיתים בסוף פסקה הפניה
לנספח/עמוד בסוגריים עגולים, כגון `(1)`, `(12)`, `(223)`. הסקריפט מסיר את
כל ההפניות הללו. דפוסי הזיהוי:

- `] (N)` בסוף ציטוט בסוגריים מרובעים - הסוגריים העגולים מוסרים.
- ` (N)` עצמאי לפני סוף משפט (`.`, `,`, `;`, `:`) - מוסר.
- תאריכים בסוגריים (`(21.1.2025)`) - נשארים, מכיוון שהם מכילים נקודות.

**שמירה על הפניות לגיטימיות.** ציטוט החוק `[נוסח משולב]` הוא חלק מהשם הרשמי
של חוקים ישראליים (למשל "חוק הביטוח הלאומי [נוסח משולב], התשנ"ה-1995") -
לכן הוא נשמר.

**זיהוי מבנה.** הסקריפט מסווג כל פסקה בקלט לאחד מארבעה סוגים:

- **כותרת-משנה** (`sub_heading`): פסקה ממורכזת + מודגשת בקלט - מקבלת סגנון
  `Heading 2`.
- **אזכור נספח** (`exhibit_ref`): פסקה שמכילה את הצירוף "מצורף ומסומן כנספח N"
  - מקבלת סגנון `List Paragraph` + numPr עם numId=0 (ביטול המספור) +
  קו תחתון על כל הפסקה.
- **סעד** (`remedy`): פסקה שמתחילה ב-"א.", "ב.", "ג.", "ד." וכו' - הקידומת
  מוסרת מהטקסט והפסקה מקבלת סגנון `List Paragraph` + numPr עם numId=43
  (hebrew1) שמייצרת אוטומטית את האותיות א, ב, ג, ד.
- **גוף** (`body`): כל פסקה אחרת - מקבלת סגנון `List Paragraph` ומתמספרת
  אוטומטית 1, 2, 3, ... דרך ירושת המספור של הסגנון (numId=14 → decimal).

**בנייה מחדש של המסמך** מבוססת תבנית `references/template.docx` שכוללת
מראש את כל הסגנונות, הגדרות המספור, ופרטי הסקציה (שוליים, RTL):

- פונט David 12pt לעברית, RTL, יישור לשני הצדדים, מרווח שורות 1.5.
- שוליים: top=2.54 ס"מ, bottom=1.9 ס"מ, left=right=3.17 ס"מ.
- כותרות-משנה: סגנון Heading 2.
- פסקאות גוף: סגנון List Paragraph עם מספור אוטומטי 1, 2, 3 (מתעדכן אם
  מוחקים פסקה ב-Word).
- אזכורי נספחים: List Paragraph עם קו תחתון (ללא מספור גלוי).
- סעדים מבוקשים: List Paragraph + hebrew1 numbering (א, ב, ג, ד).
- שמות צדדים בציטוטי פסיקה מודגשים (Bold).
- פסק דין מצוטט במלואו רק בהופעה הראשונה; כל ציטוט חוזר (בסוגריים) מוסר
  לחלוטין - הפסק כבר מבוסס, והפרוזה נושאת את שמו במידת הצורך ("הלכת X",
  "בעניין X"). כך נמנעים אזכורים תלושים מיותרים.

**עריכת התבנית.** כדי לשנות פונט/שוליים/מרווח שורות באופן גלובלי - פותחים
את `references/template.docx` ב-Word, משנים את הסגנון המתאים (Normal /
List Paragraph / Heading 2) ושומרים. השינוי יוחל אוטומטית על כל המסמכים
החדשים שייוצרו.

## פלט

הסקריפט שומר קובץ DOCX חדש ומדפיס סיכום שכולל: מספר כותרות-משנה, פסקאות
גוף, אזכורי נספחים, סעדים, וסטטיסטיקות ניקוי (LawMate mentions, em-dashes,
ציטוטים שהודגשו). שלח למשתמש קישור `computer://` לקובץ החדש עם סיכום קצר.
אל תכתוב הסבר ארוך - המשתמש בודק את המסמך בעצמו.

## איך להתאים מקרים חריגים

הסקריפט הוא הקו הראשון. אם זיהוי הפרקים יצא לא נכון, אפשר לקרוא את הקובץ
המקורי, להבין את המבנה, ולהיעזר בסקריפט כספריית פונקציות:

- `clean_text(t)` - ניקוי טקסט מ-LawMate, LRM/RLM, מקפים ארוכים, וציטוטים.
- `classify_paragraph(p)` - מחזיר `(kind, text)` כאשר kind ∈
  {sub_heading, exhibit_ref, remedy, body}.
- `extract_items(doc)` - מחלץ את כל הפסקאות המסווגות מקובץ הקלט.
- `dedup_citations(items)` - מסיר ציטוטים חוזרים (שומר את הראשון בלבד).
- `build_docx(items, output_path)` - פותח את התבנית, מוסיף פסקאות עם החלת
  סגנונות + numPr, ושומר.

המשתנים `BOLD_OPEN`/`BOLD_CLOSE` (`\x01`/`\x02`) מסמנים בטקסט מקטעים שיופיעו
כ-bold בעת ייצור ה-DOCX. הקבועים `EXHIBIT_NUMID=0`, `REMEDY_NUMID=43` מצביעים
על מספרי numId בתבנית - לא לשנות בלי לעדכן גם את `template.docx`.
